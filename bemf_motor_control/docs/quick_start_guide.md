# BEMF无传感器电机控制系统 - 快速入门指南

## 1. 准备工作

### 1.1 硬件准备

**必需硬件:**
- 8051增强型MCU开发板
- 三相无刷直流电机
- 功率驱动板 (三相桥式)
- 电源适配器 (12-48V)
- 连接线缆

**可选硬件:**
- 示波器 (调试用)
- 万用表
- USB转串口模块 (调试通信)

### 1.2 软件准备

**开发工具:**
- SDCC编译器
- Keil C51 (可选)
- 串口调试助手
- 烧录工具 (如STC-ISP)

**源代码:**
- 下载完整的源代码包
- 解压到工作目录

## 2. 硬件连接

### 2.1 基本连接

```
MCU开发板 ←→ 功率驱动板 ←→ 三相电机
     ↓
   电源模块
     ↓
   检测电路
```

### 2.2 详细连接表

| MCU引脚 | 功能 | 连接目标 |
|---------|------|----------|
| P1.0 | PWM_U_HIGH | 驱动板U相上桥臂输入 |
| P1.1 | PWM_U_LOW | 驱动板U相下桥臂输入 |
| P1.2 | PWM_V_HIGH | 驱动板V相上桥臂输入 |
| P1.3 | PWM_V_LOW | 驱动板V相下桥臂输入 |
| P1.4 | PWM_W_HIGH | 驱动板W相上桥臂输入 |
| P1.5 | PWM_W_LOW | 驱动板W相下桥臂输入 |
| P0.0 | BEMF_U | U相BEMF检测点 |
| P0.1 | BEMF_V | V相BEMF检测点 |
| P0.2 | BEMF_W | W相BEMF检测点 |
| P0.3 | CURRENT | 电流传感器输出 |
| P0.4 | VOLTAGE | 电压检测点 |
| P3.0/P3.1 | UART | 调试串口 |

### 2.3 电机相序确认

**重要**: 正确的电机相序对系统正常工作至关重要。

1. 使用万用表测量电机三相绕组阻值
2. 确认三相阻值基本相等 (误差<5%)
3. 按照U-V-W顺序连接电机

## 3. 软件编译

### 3.1 使用SDCC编译

```bash
# 进入项目目录
cd bemf_motor_control

# 编译项目
make all

# 生成的文件在dist目录下
ls dist/
```

### 3.2 使用Keil C51

1. 打开Keil C51
2. 创建新项目
3. 添加所有.c文件到项目
4. 设置包含路径为`include`目录
5. 编译项目

## 4. 参数配置

### 4.1 基本电机参数

编辑 `include/bemf_config.h` 文件:

```c
// 根据实际电机修改以下参数
#define MOTOR_POLE_PAIRS    4           // 电机极对数
#define STARTUP_DUTY        20          // 启动占空比 (%)
#define MAX_DUTY_CYCLE      90          // 最大占空比 (%)

// BEMF检测参数 (可能需要调整)
#define BEMF_THRESHOLD      512         // BEMF检测阈值
#define BEMF_FILTER_COUNT   3           // 滤波次数
```

### 4.2 保护参数设置

```c
// 根据系统规格设置保护参数
#define MAX_CURRENT         5000        // 最大电流 (mA)
#define MAX_VOLTAGE         48000       // 最大电压 (mV)
#define MAX_TEMPERATURE     85          // 最大温度 (°C)
```

## 5. 程序下载

### 5.1 使用STC-ISP (适用于STC单片机)

1. 连接MCU的串口到PC
2. 打开STC-ISP软件
3. 选择对应的MCU型号
4. 加载HEX文件
5. 点击下载

### 5.2 使用其他烧录器

根据具体的MCU型号和烧录器，按照相应的操作步骤进行。

## 6. 首次运行

### 6.1 安全检查

**在首次运行前，请务必检查:**

1. ✅ 所有连接正确无误
2. ✅ 电源电压在规定范围内
3. ✅ 电机能够自由转动
4. ✅ 保护参数设置合理
5. ✅ 串口连接正常

### 6.2 上电测试

1. **连接串口调试工具** (波特率9600)
2. **给系统上电**
3. **观察串口输出**，应该看到:
   ```
   BEMF Motor Control System Started
   ```
4. **设置目标转速** (通过修改代码或串口命令)
5. **观察电机启动过程**

### 6.3 预期现象

**正常启动过程:**
1. 系统初始化 (约1秒)
2. 电机对齐 (约50ms)
3. 开环加速 (约100ms)
4. 切换到BEMF控制
5. 稳定运行

**串口输出示例:**
```
BEMF Motor Control System Started
Motor alignment started
Open loop acceleration
BEMF detection enabled
Speed: 1500 RPM, Duty: 45%, State: 2
```

## 7. 常见问题排除

### 7.1 电机不启动

**可能原因及解决方法:**

| 问题 | 可能原因 | 解决方法 |
|------|----------|----------|
| 无PWM输出 | 程序未正确运行 | 检查程序下载，重新上电 |
| PWM输出异常 | 硬件连接错误 | 检查PWM引脚连接 |
| 电机抖动 | 相序错误 | 调整电机相序连接 |
| 立即保护 | 保护参数过严 | 适当放宽保护阈值 |

### 7.2 启动后立即停止

**检查步骤:**
1. 查看串口输出的故障信息
2. 检查保护参数设置
3. 确认电机负载不过大
4. 检查电源电压是否稳定

### 7.3 运行不稳定

**调整参数:**
1. 增加BEMF滤波次数
2. 调整BEMF检测阈值
3. 减小启动斜坡时间
4. 检查硬件噪声

## 8. 参数调优

### 8.1 BEMF检测参数

```c
// 如果电机启动困难，尝试调整这些参数
#define BEMF_THRESHOLD      512    // 降低阈值 (如400)
#define BEMF_FILTER_COUNT   3      // 增加滤波 (如5)
#define COMMUTATION_DELAY   30     // 调整延迟 (20-50us)
```

### 8.2 启动参数

```c
// 启动参数优化
#define STARTUP_DUTY        20     // 可适当增减 (15-30%)
#define STARTUP_RAMP_TIME   100    // 可适当延长 (150-200ms)
```

### 8.3 PID参数

在 `src/main.c` 中调整PID参数:

```c
// 速度控制PID参数
int pid_output = (speed_error * 2) +           // 比例项
                 (integral / 10) +             // 积分项  
                 ((speed_error - last_error) * 1); // 微分项
```

## 9. 性能测试

### 9.1 基本功能测试

1. **启动测试**: 电机能否顺利启动
2. **速度控制**: 改变目标转速，观察响应
3. **负载测试**: 增加负载，检查稳定性
4. **保护测试**: 验证各种保护功能

### 9.2 性能指标

**测试项目:**
- 启动时间: < 500ms
- 速度精度: ±1%
- 响应时间: < 100ms
- 运行效率: > 90%

## 10. 进阶功能

### 10.1 串口命令控制

可以扩展串口命令功能:
- 设置目标转速
- 查询运行状态
- 修改保护参数
- 系统复位

### 10.2 参数存储

利用EEPROM存储:
- 电机参数
- 保护设置
- 校准数据
- 运行统计

### 10.3 上位机软件

开发上位机软件实现:
- 实时监控
- 参数调整
- 数据记录
- 故障分析

## 11. 技术支持

### 11.1 文档资源

- `docs/technical_manual.md` - 详细技术手册
- `hardware/schematic_notes.md` - 硬件设计说明
- `examples/basic_usage.c` - 示例代码

### 11.2 调试技巧

1. **使用示波器观察PWM波形**
2. **监控BEMF信号质量**
3. **记录启动过程数据**
4. **分析保护触发原因**

### 11.3 常用测试点

| 测试点 | 信号 | 正常值 |
|--------|------|--------|
| PWM输出 | 方波信号 | 0-5V, 20kHz |
| BEMF信号 | 正弦波信号 | 0-3.3V |
| 电流检测 | 直流电压 | 1.65V±变化 |
| 电压检测 | 直流电压 | 与母线电压成比例 |

---

**祝您使用愉快！如有问题，请参考技术手册或联系技术支持。**